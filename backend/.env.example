PORT=3000
NODE_ENV=development

DATABASE_URL=postgres://agrosec:agrosec@localhost:5432/agrosec

JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=8h

# Token compartilhado com o security-engine (Python) para os endpoints internos
# que simulam o barramento GCP Pub/Sub -> Cloud Functions.
INTERNAL_SERVICE_TOKEN=change-me-internal-token
